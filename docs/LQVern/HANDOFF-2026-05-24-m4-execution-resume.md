# Handoff — M4 / LQVern execution: Phase C underway → resume at Task M4-C2 (web dashboard)

> **For:** the next Claude Code session, on branch **`feat/lqvern-m4-autonomous`** in **`~/Code/lq-ai`** (the canonical repo — never `~/Desktop/lq-ai`).
> **Where we are:** **Phase A + Phase B COMPLETE. Phase C underway: M4-C1 (notification dispatch) DONE** (`fa67e8f`), both review stages passed. Branch HEAD ~`fa67e8f` (+ doc commits on top). **RESUME AT Task M4-C2** — the SvelteKit web dashboard (`web/src/routes/lq-ai/autonomous/`): opt-in gate, sessions list + receipt view + Halt button, memory review (keep/edit/dismiss/delete), precedent board (read/dismiss/promote), schedule + watch config, and the notifications surface (GET/mark-read). C2 is **frontend** (OpenWebUI SvelteKit fork — NOT React; design-system primitives, not ad-hoc Tailwind) driving the now-complete `/autonomous/*` API (113 OpenAPI paths). Then **Phase D** (D1 Learn viz build per docs/LQVern/learn-tab-autonomous-flow-viz-spec.md; D2 boundary-registers R4/R5/R6→shipped flip + fresh-install acceptance + docs finalize).
>
> **M4-C1 recap (2026-05-25):** the in-app `notify` substrate already existed from A3 (table + write handler, migration 0040), so C1 added: **email transport** — stdlib `smtplib`+`EmailMessage` via `asyncio.to_thread` (`app/autonomous/notify_email.py`, NO new dep), a clean no-op when SMTP unconfigured, **bounded by `smtp_timeout` (default 10s)** so a hung mail server can't stall the delivery node; the `notify` chokepoint handler now sends a best-effort email of the in-app notification to the session user (one in-app row stays the durable record; `channel` email/webhook stay the reserved seam, webhook=DE-312). SMTP `Settings` fields in `config.py`. **Read/dismiss API** `GET /autonomous/notifications` (`?unread=true`) + `POST /autonomous/notifications/{id}/read` (read_at = the dismiss action; per-user 404-not-403). **Migration 0043** = the notifications read-index 0040 deferred, as a partial `(user_id, created_at DESC) WHERE read_at IS NULL`. R6 regression: `notify` rejected outside `Phase.delivery`. OpenAPI → 113. Code-quality review caught the missing SMTP timeout (fixed). Resolutions in plan → Task M4-C1.
>
> **M4-B4 recap (2026-05-25):** watches. **Corrected Decision M4-7's hook location** (Kevin): the watch trigger hooks **`attach_file`, NOT `ingest_file`** — `attach_file` requires `ingestion_status=='ready'` (set only at end of ingest), so the file is in zero KBs at ingest time; the real "doc arrives in a watched KB" event is the KB attach. `app/autonomous/watch_trigger.py::fire_watches_for_kb` (injectable enqueue, mirrors `_run_schedule_sweep`) spawns one session per enabled watch (trigger_kind='watch', trigger_ref, params{kb_id,target,file_id}), **owned by the watch's user**, committed-then-enqueued (avoids B3's enqueue-before-commit race); called best-effort from `attach_file` so a trigger failure never fails/rolls-back the attach (M1-path regression test pins this). `/autonomous/watches` CRUD (per-user 404-not-403; DELETE→200; POST validates KB ownership). **No new migration** (watches table + `idx_autonomous_watches_kb_enabled` from 0039; `session.params` from 0042). OpenAPI count → **111**. Two DEs filed from the code-quality review: **DE-321** (watch firing under a future KB-sharing model) + **DE-322** (validate playbook/project FK ownership on schedule+watch create). Resolutions in `docs/M4-IMPLEMENTATION-PLAN.md` → Task M4-B4.
>
> **M4-B3 recap (2026-05-25):** `/autonomous/schedules` CRUD + `autonomous_schedule_dispatcher` arq cron → `_run_schedule_sweep` spawns a session per due schedule, advances `next_run_at`, skips disabled/deleted, guards each schedule in try/except. **Migration 0042** = `params` JSONB on `autonomous_sessions` (the trigger→target seam; executor reads it into `initial_state`) + the deferred `idx_autonomous_schedules_due` index. **In-repo 5-field cron helper** `app/autonomous/cron.py` (no new dep; standard Vixie/POSIX DoM/DoW OR semantics; rejects unsatisfiable exprs at validation). Resolutions in plan → Task M4-B3.
>
> **Side items on main (all cross-team, off-M4-branch, MERGE to main independently):** PR #103 (streaming inference_routing_log fix) MERGED as **`7c7ce14`**; PR #104 (observability.md span/attr accuracy) MERGED as **`9672ef4`**; PR #105 (`/v1/models` OpenAPI now declares the alias fields `lq_ai_resolves_to`, `lq_ai_fallback_count`, tier-on-aliases the gateway already returns; doc-only + a property-superset conformance test) MERGED as **`438198c`**. Flagged code-side follow-up: `gateway/app/observability.py:83` docstring claims an `inference_requests_total` outcome `refused` that is never emitted.
>
> **Phase B remaining (per docs/M4-IMPLEMENTATION-PLAN.md):** B2 precedent board (write/read/dismiss + `/autonomous/precedents`; `precedent_entries` table exists from A1; per-user, like B1's memory API — mirror `api/app/api/autonomous.py` memory endpoints + `_load_owned_*` + `audit_action` resource_type="precedent_entry"). B3 scheduled tasks (arq cron on the shared worker — mirror the A4-ii watchdog cron registration; `autonomous_schedules` table exists from A1; **the deferred `autonomous_schedules` index lands HERE** per the A1 decision; + `/autonomous/schedules` CRUD). B4 watches (KB-arrival trigger via **direct arq-enqueue from `ingest_file`** — best-effort, must NOT fail ingest; `autonomous_watches` table exists from A1; + `/autonomous/watches` CRUD). Each gets a per-user isolation test + OpenAPI conformance. B2-B4 POPULATE the `params` the A3.3b `_dispatch` handlers consume. Then Phase C (C1 notify, C2 web dashboard), Phase D (D1 Learn viz, D2 boundary-registers + fresh-install acceptance).
> **How to run it:** subagent-driven, one task per focused session, TDD, two-stage review (spec then code-quality). The plan is the task spec.
>
> **When cleared for Phase B (per docs/M4-IMPLEMENTATION-PLAN.md):** B1 per-user memory + `/autonomous/memory` API (the `propose_memory` write side already exists from A3.3b — B1 adds the keep/dismiss curation API); B2 precedent board + `/autonomous/precedents`; B3 scheduled tasks (arq cron, + the deferred `autonomous_schedules` index lands here per the A1 decision); B4 watches (KB-arrival trigger, direct arq-enqueue from `ingest_file`, best-effort, must NOT fail ingest). Each primitive gets a per-user isolation test. These POPULATE the `params` the A3.3b `_dispatch` handlers consume (kb_id/playbook_id/skill_ref/model/messages). Then Phase C (C1 notify email/read-API/webhook — the in-app substrate + write already exist from A3.2/A3.3b; C2 web dashboard), Phase D (D1 Learn viz, D2 boundary-registers flip + fresh-install acceptance).
>
> **Phase A deliverables map:** `api/app/autonomous/{enums,state,nodes,phases,executor,cost,audit,guard,receipt}.py` + `api/app/api/autonomous.py` (3 endpoints) + `api/app/workers/{autonomous_worker.py (job+watchdog),arq_setup.py (cron registered)}` + migrations `0039` (5 tables) + `0040` (autonomous_notifications) + schemas in `app/schemas/autonomous.py`. Tests under `api/tests/autonomous/` (brakes, executor_skeleton, guard_helpers, observability, sessions_api, idle_watchdog) + `api/tests/models/test_autonomous_{models,notifications}.py`. Migration-rebuild rule: when 0039+0040 deploy, rebuild api+arq-worker+ingest-worker together.

---

## NEXT SESSION — M4-C2 (web dashboard): **DISCUSS the layout with Kevin FIRST, then build**

Kevin's instruction: do **not** jump straight into building C2. Open with a conversation to settle the dashboard's information architecture and per-surface UX, then build. This is the whole-backend payoff surface (every `/autonomous/*` API now exists — sessions, memory, precedents, schedules, watches, notifications, promote-proposals; 113 OpenAPI paths). The plan's task spec is **`docs/M4-IMPLEMENTATION-PLAN.md` → Task M4-C2**.

**Frontend, not the backend loop.** C2 is SvelteKit in the OpenWebUI fork (`web/`) — **SvelteKit, NOT React; design-system primitives, not ad-hoc Tailwind** (CLAUDE.md web rules). The backend subagent-driven TDD loop does NOT transfer; instead: brainstorm → (optionally) throwaway HTML/sketch mockups to settle layout → build the SvelteKit routes mirroring existing conventions → `npm run check` (svelte-check) + `npm run lint` + Cypress E2E. Consider the **`superpowers:brainstorming`** skill to open, and **`frontend-design`** / a sketch playground for the layout.

**Read before the discussion:** Task M4-C2 in the plan; PRD §3.10 (off-by-default, **opt-in per user**; the dashboard *renders receipts, does NOT run the loop* — ADR 0013 D2: web layer is presentation only). Existing web patterns to mirror: `web/src/routes/lq-ai/` (esp. `admin/intake-bridges/+page.svelte` — a recent list+actions CRUD page, the closest template), `web/src/lib/lq-ai/components/` (e.g. `ModelPicker.svelte`), and the api-client layer `web/src/lib/lq-ai/api/` (e.g. `models.ts`) — C2 adds an `autonomous.ts` client wrapping the `/autonomous/*` endpoints.

**Discussion agenda — the layout/UX decisions to settle before building:**
1. **IA / navigation.** One "Autonomous" area with sub-pages (Sessions · Memory · Precedents · Schedules · Watches · Notifications) vs a single dashboard with panels? How it slots into OpenWebUI nav (sidebar entry? under an existing lq-ai menu? admin-style layout like intake-bridges?).
2. **Opt-in gate.** Where the per-user toggle lives (user settings?), the pre-opt-in empty state / explainer, and how every surface stays **unreachable** until opted in (the verification pins this).
3. **Sessions + receipt view** — the headline UX (*"audit exactly what the agent did and why"*). How to render the receipt: phases → per-tool-call (intent / cost / outcome / gate decisions) → terminal state. Timeline vs expandable table? The **Halt** button on a running session (calls `POST …/halt`) + confirm.
4. **Memory review** — proposed / kept / dismissed (columns or filtered list) + keep / edit-on-keep / dismiss / delete controls (D4: the user owns it).
5. **Precedent board** — read + dismiss, and the **promote→Project** flow (B2 built the full lifecycle: `promote` creates a `project_context_proposals` row → user `accept` appends to `projects.context_md`, `reject` discards). How to surface promote + the proposal review/accept UI.
6. **Schedules + watches config** — create/edit forms: cron-expression input with validation feedback (the API 422s invalid/unsatisfiable crons); KB picker (watches); playbook/skill target picker; enable/disable toggle; soft-delete.
7. **Notifications** — bell + unread badge vs a dedicated page; the mark-read interaction (`POST …/{id}/read`, `?unread=true` list); email is already sent server-side (C1), this is the in-app surface.
8. **v1 scope** — which surfaces are full CRUD vs read+toggle; what (if anything) defers to a DE to keep C2 shippable in its 18–24h envelope.

**Build/verify (after the layout is agreed):** `web/src/routes/lq-ai/autonomous/` routes + an `autonomous.ts` api client; `cd web && npm run check` + `npm run lint` clean; Cypress E2E (opt-in, receipt view, halt, memory keep, precedent dismiss); visual smoke that surfaces are hidden pre-opt-in. After C2: **Phase D** (D1 Learn viz build per `docs/LQVern/learn-tab-autonomous-flow-viz-spec.md`; D2 boundary-registers R4/R5/R6→shipped flip + fresh-install acceptance + docs finalize).

---

## 0. Start here (orientation, in order)

1. `CLAUDE.md` — conventions + decision routing (read if unfamiliar).
2. **`docs/M4-IMPLEMENTATION-PLAN.md`** — THE task list. Your next task is **M4-A2**; its full Scope/Dependencies/Output/Verification is there. Read the "Architectural decisions locked for M4" section (M4-1…M4-10) — **do not re-litigate these.**
3. **`docs/adr/0013-autonomous-layer-design-influences.md`** — the design (D1–D6). Now on `main`.
4. **`docs/LQVern/agentic-flow-alignment-guide.md`** — the chokepoint shape + OTel/audit rules. M4-A2 builds the skeleton that M4-A3's chokepoint plugs into.
5. The M4-A1 output you build on: `api/app/models/autonomous.py`, `api/app/schemas/autonomous.py` (the shared `StrEnum`s live here), `api/alembic/versions/0039_autonomous_layer.py`.
6. Patterns to mirror: `api/app/playbooks/{executor,nodes,state}.py` (the closest analog — M4-A2 mirrors it) and the arq worker `api/app/workers/arq_setup.py` (`WorkerSettings.functions`).

---

## 1. Status (as of 2026-05-24, end of the writing-plans + Phase-A-kickoff session)

- **Design on `main`:** PR **#100** merged (`main` HEAD `c1a4296`) — ADR 0013 + PRD §3.10 build-out + alignment guide. `de265.patch` was dropped (dead content = DE-289) and its references trimmed. `main` no longer lags the pinned design.
- **Plan + viz spec written** on the LQVern branch: `docs/M4-IMPLEMENTATION-PLAN.md` (13 tasks / 5 phases) + `docs/LQVern/learn-tab-autonomous-flow-viz-spec.md` (build sequenced as Task M4-D1; spec-only so far).
- **Branch `feat/lqvern-m4-autonomous` HEAD = `e40f945`**, pushed to **origin + tucuxi**, working tree clean.
- **M4-0.1 DONE** (`33c5d77`): langgraph held at `>=0.2.76,<0.3`. Dependabot **#68 closed** (its break is mypy-only — 1.x re-types `StateGraph` as generic so our `add_node` factories fail `[call-overload]`; runtime APIs unchanged; the autonomous executor needs no 1.x API). **DE-319** filed (migrate playbooks + tabular + autonomous executors to 1.x post-M4 — note `tabular/executor.py` is a *third* langgraph consumer the plan didn't originally name).
- **M4-A1 DONE** (`e40f945`): the 5-table data substrate. Migration `0039_autonomous_layer.py` (`down_revision=0038`): `autonomous_sessions` (+ brakes) / `autonomous_schedules` / `autonomous_watches` / `autonomous_memory` / `precedent_entries`, all per-user FK `ON DELETE CASCADE`. SQLAlchemy models + Pydantic schemas (shared `StrEnum`s = exact CHECK values) + `db-schema.md` section. **27 model tests pass; alembic round-trips; ruff + mypy clean.** Both review stages cleared.

## 2. The next task — M4-A2 (executor skeleton)

Build `api/app/autonomous/{__init__,executor,nodes,state,enums}.py` mirroring `api/app/playbooks/`:
- The `Phase` / `ToolIntent` / `HaltState` enums + the `PHASE_GRANTS` map (exact values in the plan + alignment guide §3). **Reuse the `StrEnum`s already defined in `api/app/schemas/autonomous.py` from M4-A1** — don't redefine `Phase`/`HaltState`; add `ToolIntent` + `PHASE_GRANTS`.
- The LangGraph `StateGraph` over a typed `AutonomousSessionState`, walking `intake → analysis → drafting → ethics_review → delivery`.
- `run_phase_transition(...)` writing the `autonomous_session.phase_transition` audit row.
- **`guarded_tool_call` is STUBBED here** (raises `NotImplementedError`) — the real chokepoint with R4/R5/R6 + OTel + audit is **M4-A3** (the load-bearing task). The skeleton test asserts the stub raises (proving no tool path bypasses the chokepoint-to-be).
- Register `autonomous_session_job` in `api/app/workers/arq_setup.py::WorkerSettings.functions` (shares the M3 playbook queue; lower priority than interactive use).

Full Scope/Verification: `docs/M4-IMPLEMENTATION-PLAN.md` → Task M4-A2.

## 3. Decisions locked in Phase A so far (honor; don't re-decide)

- UUID PK default is **`gen_random_uuid()` (v4)** — matches every shipped migration; the `uuid_generate_v7()` helper is not installed (db-schema's "v7" line is aspirational). Documented in the new db-schema section.
- CHECK-constraint names use the **`chk_`** prefix (the codebase ORM convention); FK `fk_…`, index `idx_…`.
- `project_id` FK is **`ON DELETE SET NULL`** across sessions/schedules/watches.
- `autonomous_schedules` index is **deferred to M4-B3** (when the scheduler's `next_run_at` scan query shape is concrete) — noted in the migration.
- langgraph stays **`>=0.2.76,<0.3`** for all of M4 (migration is DE-319, post-M4).
- **M4-A2 DONE** (`dad059e`): executor skeleton — LangGraph phase machine + typed state + `ToolIntent`/`PHASE_GRANTS` + `run_phase_transition`; `guarded_tool_call` stubbed (→ A3); arq `autonomous_session_job` on the shared `M3_PLAYBOOK_QUEUE_NAME`. Two-stage review fixed a real data-loss bug (happy path never committed → worker's `async with factory()` rolled back every successful run; row stuck at `running`) + the state-dict error path + `completed_at`. 37 tests pass. Pushed origin + tucuxi.
- **M4-A3 `_dispatch` scope = WIRE EVERYTHING REAL** (Kevin's call): all six `ToolIntent` handlers route to real downstreams in A3, not stubs — `retrieve_chunks`→`app.knowledge.hybrid_search`, `run_skill`/`run_playbook`→`GatewayClient`, `propose_memory`→real `autonomous_memory` row write (ahead of B1's API), `emit_finding`→session state/result.
- **`notify` in A3 = REAL IN-APP ONLY** (Kevin's call): A3 builds a notifications table + migration `0040` + model + an in-app persist handler so `notify` writes a durable artifact. **Email/SMTP transport + the read/dismiss API + web surface stay in M4-C1.** This is a deliberate, bounded C1 pull-forward (the in-app data model + write side only). C1's scope shrinks accordingly.
- **A3 is being executed as four reviewed sub-dispatches** (A3.1 errors+cost+audit ✅ `9774c33`; A3.2 notifications model+migration 0040 ✅ `df6f575`; A3.3a chokepoint guard.py + R4/R5/R6 brakes + OTel + audit + local handlers (emit_finding/propose_memory/notify) + brake tests ✅ `66da966`; **A3.3b NEXT** = real retrieve_chunks/run_skill/run_playbook handlers + executor wiring + privacy-guard test) — execution mechanics for an over-large task; the plan still treats A3 as one task.
- **A3.3b handler depth (anchored in the plan, not re-litigated):** inference intents `run_skill`/`run_playbook` make **GatewayClient chat-completion calls** (mirroring `api/app/playbooks/nodes.py`'s gateway path) — NOT nested playbook-executor sub-flows. `retrieve_chunks`→`app.knowledge.hybrid_search`. Handlers are **param-driven** (kb_id / model / messages / playbook_id arrive via `params`); the trigger→target resolution that POPULATES those params is Phase B (B3/B4). The privacy-guard test supplies a real KB+document with synthetic PII and asserts no raw value lands in any `autonomous.*` span attr or audit `details`. **M4-A3 COMPLETE** — A3.3b ✅ `52cfd1a` (real handlers + executor wiring + privacy guard). Review-driven additions: `ToolResult.outcome` (gateway transport error audits `outcome="gateway_error"`, NOT `success` — the call is still charged the R4 estimate); brake→terminal-status mapping in executor (`SessionHalted`/`CostCapReached`→`halted`, `ToolNotGranted`→`failed`); `data["summary"]` (audit-safe IDs/counts/offsets) vs `data["chunks"]` (full text, node-LLM-only, never logged); `anonymize=True` default for autonomous inference. 47 autonomous tests pass.
- **Known minor (deferred, not blocking):** `_dispatch` reads required `params` keys (`kb_id`/`query`/`model`/`messages`/`category`/`title`/`body`) without `.get()` — a missing key raises `KeyError`→executor `status="failed"` (correct failure mode; reviewer rated Minor; the trigger→param plumbing in Phase B is the closed caller set). Consider typed per-intent param models if `_dispatch` grows.
- **M4-A4 DONE** (Phase A complete): A4-i `d3a8fed` — `receipt.py::build_receipt` (audit-derived, JSONable, no raw values, persisted to `session.result` at delivery) + 3 endpoints (`POST /sessions/{id}/halt` idempotent + per-user 404 + `autonomous_session.halt_requested` audit [added to the closed `_ACTIONS` set]; `GET /sessions` paginated newest-first; `GET /sessions/{id}` + receipt) + OpenAPI/conformance. A4-ii `05099a0` — `autonomous_idle_watchdog` arq cron on the shared `WorkerSettings.cron_jobs` (every minute, `second=0`); per-session `idle_halt_minutes`; two-tick `running→paused→halted` (paused→halted needs a SECOND idle interval; paused→halted scan runs FIRST so no single-tick double-transition); `halted` audit reason `idle_timeout`; testable core `_run_idle_sweep(db, *, now=None)`. Both review-approved.
- **Unrelated fix shipped to main (separate branch):** `chat_receipts.py` inference/error `detail` was missing `anonymization_applied` + `message_id` (both already columns on `InferenceRoutingLog`) — added for another project's UI anonymization indicator. Branch `fix/chat-receipts-inference-detail-fields`, commit `4aa7605`, **PR #102** (additionalProperties:true → no schema change; regression test added). Awaiting Kevin's merge; he passes the merge SHA to the other CC.
- **Brake-commit contract (documented in guard.py + executor.py + nodes.py docstrings):** an `AutonomousBrake` mutates session + flushes an audit row then raises WITHOUT committing; the executor's terminal `except` commits. A node that catches a brake locally MUST commit or the latch+audit row are lost (the A2 data-loss class). A3.3b nodes let brakes propagate.
- **HONEST-STATE / #99 (separate branch):** issue #99 (dead `paddleocr` placeholder breaking `--profile local`; OCR never implemented) fixed on branch `fix/issue-99-local-profile-paddleocr` → **PR #101** to main. Filed **DE-320** (scanned-PDF OCR). Note: DE-320 is on that branch, not the M4 branch; DE-319 (langgraph 1.x migration) is on the M4 branch. No collision.

## 4. How to execute (the workflow Kevin chose)

- **Subagent-driven, per task** (`superpowers:subagent-driven-development`): dispatch a fresh implementer subagent with full task text + scene-setting (don't make it read the plan file — paste the task), then **spec-compliance review first, then code-quality review**; the implementer fixes any issues; re-review; mark complete. The controller (you) does not pause for "should I continue?" between *sub-steps* — but **Phase A is a hard gate before Phase B**, and the plan's "one task per focused session" means a natural checkpoint per task is fine.
- **TDD** (red→green). New endpoints (later tasks) get unit + integration + OpenAPI-conformance tests.
- **Gates:** `ruff format` AND `ruff check` (separately — CI runs both), `mypy` (api standard). **DCO sign-off** (`git commit -s`) + the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer. Repo git identity is already correct (`Kevin-Tucuxi <kevin@tucuxi.ai>`) — don't override it.
- **Push to BOTH remotes** after each task: `git push origin feat/lqvern-m4-autonomous && git push tucuxi feat/lqvern-m4-autonomous`. Never delete merged branches.

## 5. Gotchas / environment

- **Local test DB:** read `POSTGRES_PASSWORD` from repo-root `.env`; `cd api && DATABASE_URL="postgresql+asyncpg://lq_ai:<pw>@127.0.0.1:15432/lq_ai" ./.venv/bin/pytest … -q` (conftest makes a throwaway test DB — safe). The venv is `api/.venv`.
- **Migration-rebuild rule:** migration `0039` is committed but **not yet deployed**. When it deploys to a running stack, rebuild **api + arq-worker + ingest-worker together** or the stale siblings crash-loop on "Can't locate revision `0039`."
- **Inference always goes through the gateway** (`app.clients.gateway.GatewayClient`) — never a provider SDK from `api/`. This is what gives autonomous flows anonymization + tier + cost for free.
- `.gitignore build/` shadows `web/src/routes/lq-ai/learn/build/` — edits there need `git add -f` (relevant only at M4-D1).

## 6. Precise kickoff message to paste into the next session

> Continue M4 / LQVern execution on LQ.AI in `~/Code/lq-ai` (the canonical repo — **never** `~/Desktop/lq-ai`; the Bash cwd resets to ~/Desktop between calls, so prefix every command with `cd ~/Code/lq-ai &&`), on branch `feat/lqvern-m4-autonomous`. **Read `docs/LQVern/HANDOFF-2026-05-24-m4-execution-resume.md` first** — it has full state. Phase A is COMPLETE and **Phase B is underway: M4-B1 (per-user memory API) is done and pushed** (branch HEAD `3557bf9`). **Resume at Task M4-B2** (precedent board + `/autonomous/precedents` API) per `docs/M4-IMPLEMENTATION-PLAN.md`, then **M4-B3** (scheduled tasks — arq cron + `/autonomous/schedules`; the deferred `autonomous_schedules` index lands here) and **M4-B4** (watches — direct arq-enqueue from `ingest_file`, best-effort, must NOT fail ingest; + `/autonomous/watches`). Run each subagent-driven (fresh implementer with full pasted task text → spec-compliance review → code-quality review → fix → re-review), strict TDD (red→green), one task per focused session. **Mirror the proven Phase-A/B1 patterns:** extend `api/app/api/autonomous.py` (the `_load_owned_*` 404-not-403 idiom, pagination envelope, `audit_action` with `resource_type=` per primitive); the tables already exist from M4-A1 (`precedent_entries`, `autonomous_schedules`, `autonomous_watches`); cron registration mirrors the A4-ii idle-watchdog in `arq_setup.py`/`autonomous_worker.py`. New endpoints get unit + integration + **OpenAPI-conformance** tests (update `docs/api/backend-openapi.yaml` + the `EXPECTED_PATHS`/count in `api/tests/test_openapi.py`). **Gates:** `ruff format` AND `ruff check` (separately), `mypy` (api standard); **DCO** `git commit -s` + the `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` trailer; **push BOTH remotes** after each task (`git push origin feat/lqvern-m4-autonomous && git push tucuxi feat/lqvern-m4-autonomous`). Honor the locked decisions M4-1…M4-10 + the Phase-A/B decisions in handoff §3 — don't re-litigate. **Stop and ask** on any architectural question not anchored in the design (ADR 0013 / PRD §3.10 / `docs/LQVern/agentic-flow-alignment-guide.md`). Local test DB: read `POSTGRES_PASSWORD` from repo-root `.env`, then `cd api && DATABASE_URL="postgresql+asyncpg://lq_ai:<pw>@127.0.0.1:15432/lq_ai" ./.venv/bin/pytest tests/autonomous/ tests/test_openapi.py -q` (conftest makes a throwaway test DB — safe). Watch the **DELETE/204 FastAPI pitfall** (CLAUDE.md) — prefer returning 200 with the entity.

---

*Drafted 2026-05-24 at the Phase-A-kickoff checkpoint. Memory `project_lq_ai_status` carries the same state; this doc is the focused execution-resume brief.*
