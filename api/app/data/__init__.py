"""Package-bundled data files (Office Add-in manifest template, etc.).

This package exists so `importlib.resources` can locate the bundled
data files inside the api image. The actual data files live alongside
this `__init__.py`.
"""
